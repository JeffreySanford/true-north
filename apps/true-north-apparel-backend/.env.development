# Development environment variables
# Use a test key placeholder (replace with a real test key when needed)
STRIPE_SECRET_KEY=sk_test_placeholder_xxx
# Leave MONGO_URI empty in development to use an in-memory MongoDB for fast local dev.
# If you want to use a real local MongoDB instead, set MONGO_URI to your mongodb://... URI.
# MONGO_URI=
# Default dev port. The backend `main.ts` reads PORT and will default to 3000 if unset.
PORT=3000
