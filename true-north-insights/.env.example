# =================================================
# TRUE NORTH INSIGHTS - ENVIRONMENT CONFIGURATION
# =================================================
#
# This file serves as a template for the environment variables required by the application.
# Copy this file to a new file named .env and fill in the appropriate values for your environment.
# The .env file is ignored by Git and should not be committed to version control.

# -------------------------------------------
# DATABASE CONFIGURATION (PostgreSQL)
# -------------------------------------------
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_NAME=truenorth
DATABASE_SSL_MODE=prefer

# -------------------------------------------
# API KEYS & AUTHENTICATION
# -------------------------------------------
JWT_SECRET_KEY=
GOOGLE_MAPS_API_KEY=
OPENAI_API_KEY=

# -------------------------------------------
# FRONTEND BUILD & PERFORMANCE
# -------------------------------------------
# These variables are used by the esbuild configuration to enforce performance budgets.
# See: /tools/build/esbuild.config.ts

# Maximum size for the initial JS bundle (main.js + polyfills.js) in Megabytes.
MAX_INITIAL_BUNDLE_SIZE_MB=2

# Maximum size for any single lazy-loaded JavaScript chunk in Kilobytes.
MAX_LAZY_CHUNK_SIZE_KB=500

# Performance target for production builds in milliseconds.
BUILD_PERFORMANCE_TARGET_MS=45000

# Output directory for the build.
BUILD_OUTPUT_DIR=dist/frontend
